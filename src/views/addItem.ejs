<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title><%=title%></title>

  <!-- Bootstrap CSS -->
  <link href="css/bootstrap.min.css" rel="stylesheet">

</head>

<body>
  <header>
    <!-- Navigation bar -->
    <nav class="navbar navbar-expand-lg navbar-light">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <%for(let i=0;i<nav.length;i++) {%>
            <%if(i === 0) {%>
                <a class="nav-item nav-link active" href="<%=nav[i].link%>"><%=nav[i].name%> <span class="sr-only">(current)</span></a>
            <%} else {%>
                <a class="nav-item nav-link" href="<%=nav[i].link%> "><%=nav[i].name%></a>
            <%}%>
          <%}%>
        </div>
      </div>
    </nav>
    <!-- /Navigation bar -->
  </header>

  <div class="py-5">
    <div class="row">
      <div class="col-md-6 col-sm-6 m-auto">
        <div class="card rounded-0">
          <div class="card-header text-center">
            <h1><%=title%></h1>
          </div>
          <div class="card-body">
            <form class="form" action="/add-item" method="post" enctype="application/x-www-form-urlencoded">
              <div class="form-group">
                <label for="name" class="form-label">Item Name</label>
                <input type="text" name="name" id="name" class="form-control">
              </div>
              <div class="row">
                <div class="form-group col-md-6">
                  <label for="isItVeg" class="form-label">Is it veg?</label>
                  <select class="form-control" name="isItVeg" id="isItVeg">
                    <option value="" disabled selected>Select an option</option>
                    <option value="yes">Yes</option>
                    <option value="no">No</option>
                  </select>
                </div>
                <div class="form-group col-md-6">
                  <label for="measurement" class="form-label">Measurement Used</label>
                  <select class="form-control" name="measurement" id="measurement">
                    <option value="" disabled selected>Select an option</option>
                    <option value="weight">weight</option>
                    <option value="number">number</option>
                  </select>
                </div>
                <div class="form-group col-md-6">
                  <label for="quantity" class="form-label">Ideal Quantity</label>
                  <select class="form-control" name="quantity" id="quantity">
                    <option value="" disabled selected>Select an option</option>
                    <option value="100">100</option>
                    <option value="150">150</option>
                    <option value="1">1</option>
                  </select>
                </div>
                <div class="form-group col-md-6">
                  <label for="unit" class="form-label">Basic Unit</label>
                  <select class="form-control" name="unit" id="unit">
                    <option value="" disabled selected>Select an option</option>
                    <option value="grams">grams</option>
                    <option value="number">number</option>
                  </select>
                </div>
              </div>
              <div class="row">
                <div class="form-group col-md-6">
                  <label for="price" class="form-label">Price</label>
                  <input type="number" name="price" id="price" class="form-control">
                </div>
                <div class="form-group col-md-6">
                  <label for="imageURL">Image URL</label>
                  <input type="text" name="imageURL" id="imageURL" class="form-control">
                </div>
              </div>
              <button class="btn btn-primary btn-block" type="submit" class="form-control">Add Food Item</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="mt-5">
    <div class="row m-5">
      <% if(hasAnythingAdded) { %>
        <% for(let i=0;i<foodItems.length;i++) { %>
          <div class="card col-md-3 mb-3" style="width: 18rem;">
            <img src="<%= foodItems[i].imageURL %>" class="card-img-top mt-1 ml-1" style="width: 100%; height: 10rem;" alt="">
            <div class="card-body">
              <h5 class="card-title"><%= foodItems[i].name %></h5>
              <p class="card-text">Rs. <%= foodItems[i].price %> for <%= foodItems[i].quantity %> <%= foodItems[i].unit === 'number'? '' : foodItems[i].unit %></p>
              <a href="#" class="btn btn-primary">Update</a>
            </div>
          </div>
        <% } %>        
      <% } %>
    </div>
</div>

  <!-- Bootstrap Javascript, jQuery, and Popper.js -->
  <script src="js/jquery-3.5.1.slim.min.js"></script>
  <script src="js/popper.min.js"></script>
  <script src="js/bootstrap.min.js"></script>
</body>

</html>
